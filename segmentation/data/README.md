This directory is a stub for where the actual data is.

At the moment, the code I've written assumes the data is in this directory and in this format:

````
data/
├── images/
│   ├── train/
│   └── test/
└── masks/
    ├── train/
    └── test/
````